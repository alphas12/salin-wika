# AI Amended: Keep only direct runtime dependencies and prefer the CUDA 12.8 PyTorch wheel where supported.
--extra-index-url https://download.pytorch.org/whl/cu128

polars==1.43.2
PyYAML==6.0.3
sacrebleu==2.6.0
spacy==3.8.15
torch==2.11.0
